{
 "generated_at": "2026-09-01T00:00:00Z",
 "dependency_sources": [
  "backend/requirements.txt",
  "frontend/package.json",
  "frontend/package-lock.json"
 ],
 "runtime_dependencies": {
  "backend": {
   "firebase_admin": {
    "version": "7.5.0",
    "type": "pip",
    "purpose": "Firebase Admin SDK — Realtime DB、Storage、Auth"
   },
   "Flask": {
    "version": "3.1.3",
    "type": "pip",
    "purpose": "Web 框架"
   },
   "Flask_Login": {
    "version": "0.6.3",
    "type": "pip",
    "purpose": "使用者登入管理 (in-memory, workers=1)"
   },
   "pandas": {
    "version": "3.0.5",
    "type": "pip",
    "purpose": "資料處理 (元素爬蟲 / Realtime DB DataFrame)"
   },
   "pydantic-settings": {
    "version": "2.15.0",
    "type": "pip",
    "purpose": "環境變數設定管理"
   },
   "python-dotenv": {
    "version": "1.2.3",
    "type": "pip",
    "purpose": ".env 檔案載入"
   },
   "Pyrebase4": {
    "version": "4.9.0",
    "type": "pip",
    "purpose": "Firebase Client 認證"
   },
   "Requests": {
    "version": "2.34.2",
    "type": "pip",
    "purpose": "HTTP 請求 (PubChem API)"
   },
   "gunicorn": {
    "version": "26.2.0",
    "type": "pip",
    "purpose": "WSGI 生產伺服器 (workers=1)"
   },
   "numpy": {
    "version": "2.4.6",
    "type": "pip",
    "purpose": "app/watermark.py 的像素運算"
   },
   "gevent": {
    "version": "26.8.0",
    "type": "pip",
    "purpose": "gunicorn worker class（issue #28）"
   },
   "pillow": {
    "version": "12.3.0",
    "type": "pip",
    "purpose": "圖片處理與浮水印"
   }
  },
  "frontend": {
   "axios": {
    "version": "^1.7.0",
    "type": "npm",
    "purpose": "HTTP 客戶端 (VITE_API_URL=/api)"
   },
   "echarts": {
    "version": "^5.5.0",
    "type": "npm",
    "purpose": "雷達圖視覺化"
   },
   "vue": {
    "version": "^3.4.0",
    "type": "npm",
    "purpose": "前端框架"
   },
   "vue-router": {
    "version": "^4.3.0",
    "type": "npm",
    "purpose": "前端路由"
   },
   "firebase": {
    "version": "^12.18.0",
    "type": "npm",
    "purpose": "Google 帳號登入（signInWithPopup）。以 await import() 動態載入，不進主 bundle"
   }
  }
 },
 "dev_dependencies": {
  "frontend": {
   "@vitejs/plugin-vue": {
    "version": "^5.0.0",
    "type": "npm",
    "purpose": "Vite Vue 插件"
   },
   "vite": {
    "version": "^5.4.0",
    "type": "npm",
    "purpose": "前端建置工具"
   }
  }
 },
 "removed": {
  "backend": {
   "flask_sqlalchemy": "移除 SQLite，改用 Firebase Realtime DB",
   "SQLAlchemy": "移除 SQLite，改用 Firebase Realtime DB",
   "Flask-Cors": "CORS 是 app/__init__.py 手寫的，該套件從未被 import"
  }
 },
 "notes": "backend/requirements.txt 自 2026-09-01 起連間接依賴一起釘死（共 67 筆），本檔只列直接依賴。更新方式見 requirements.txt 開頭的註解（用 python:3.11-slim 跑 pip freeze）。前端以 package-lock.json 為準，Dockerfile 與 render.yaml 都改用 npm ci。"
}
